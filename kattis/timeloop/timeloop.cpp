#include <bits/stdc++.h>

using namespace std;

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(0);

    short int n;

    cin >> n;

    for(int i = 0; i < n; ++i) {
        cout << i + 1 << " Abracadabra\n";
    }

    return 0;
}
